{
  "name": "canvas-scraper",
  "version": "1.0.0",
  "description": "Canvas course scraper using Playwright with Firefox",
  "main": "scraper.ts",
  "scripts": {
    "scrape": "ts-node scraper.ts",
    "test": "ts-node test.ts",
    "build": "tsc scraper.ts",
    "install-browsers": "npx playwright install firefox"
  },
  "dependencies": {
    "playwright": "^1.35.0",
    "ts-node": "^10.9.0",
    "typescript": "^4.9.0",
    "@types/node": "^18.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
